{
  "name": "wellness-chatbot-widget",
  "version": "0.1.0",
  "private": true,
  "description": "Storefront chat widget for the Wellness World assistant. Vanilla TypeScript, no framework — it loads on every storefront page.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
